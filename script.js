function getFormvalue() {
    //Write your code here
	
	// Concatenate the first and last name
	let fullName = `${form1.fname} ${form1.lname}`;
	
	// Use alert() to display the full name
	alert(fullName);

}
